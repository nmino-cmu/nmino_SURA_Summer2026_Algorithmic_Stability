# Jul 30 triage (written Jul 27)

## Must ship (mentor packet)
1. Frozen writeup PDF (already) + mentor one-pager `notes/2026-07-27_mentor_onepager.pdf`
2. Honest theorem chain: (2_trade) + randomized design rates + Infl at fixed classical α
3. Notation dictionary (`notation_quarantine.md`) — do not “fix” writeup quirks
4. Clear (FV)/(OV) domain: score-threshold validity only for `θ ≥ F^{-1}(1-α)`

## Corrected Jul 27 (do not present old versions)
- Blanket (FV) over `[0,1]` for `C_θ={s≤θ}` was **false** → fixed to `Θ_val` / (OV)
- Ass.conc pathwise `Θ₀={g*≤0}` overstated for tradeoff → Hausdorff / `Θ₀^ε` primary
- Infl demo now uses conservative `ĝ ≤ -ε` feasible set
- Nest Rem.FV no longer falsely cites CRC for every fixed θ

## Present as candidate, not finished W3
- Live rates are ZJ `(η,0,ν)`-stability of **randomized** selection (W5 methodology)
- LOO of hard `(2_trade)` stated as `O_P(1/n)` lemma — sketch, not full proof
- PTO / inverse-opt structured noise: named only

## Defer past Jul 30 unless mentor asks
- Full LOO proof under strong convexity
- Inverse-opt structured noise theorems
- Woody HT / discontinuity agenda
- Recover July 2 Overleaf body

## Demo status (fresh run Jul 27)
All `research/experiments/*.py` **PASS**.
