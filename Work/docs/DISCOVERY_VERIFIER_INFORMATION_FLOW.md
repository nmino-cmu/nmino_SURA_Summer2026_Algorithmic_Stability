# Discovery ↔ Verifier Information Flow (pointer)

**Canonical document:** [`architecture-visual/DISCOVERY_VERIFIER_INFORMATION_FLOW.md`](../architecture-visual/DISCOVERY_VERIFIER_INFORMATION_FLOW.md)

Interactive canvas: `canvases/discovery-verifier-bridge.canvas.tsx` (Cursor Canvas).

Covers System A (Discovery Assistant) ↔ System B (Verification Architecture) crossing via sealed `CandidateResearchPackage`, read-only return path (`VerifierPrior`), and illegal crossings.
