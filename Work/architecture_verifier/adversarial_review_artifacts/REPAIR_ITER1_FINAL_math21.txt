Iteration 1 is mathematically insufficient to enforce the declared chain. The typed-payload safeguards are useful, but several mandatory lineage and type distinctions are absent.

## CRITICAL

- **The front half of the fixed chain has no canonical representation.** The declared chain includes `data → F_D scores → Q_ψ → selection`, but the `Claim.chain_segment` enum begins at `perturbation/selection_stability`; there are no canonical data, score-family, \(Q_\psi\), or selection-application objects and no required digest-bound transitions between them. `DataDependenceRecord` records provenance only, while `SelectionOperator` describes an operator rather than its application to \(Q_\psi\). Consequently, a stability subject can be introduced without proving that it arose from the required preceding stages. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:162>)

- **Composition does not compose certificates.** A composition Claim requires only “composition input subject digests,” not input stability-certificate Claim digests, dependencies, or `EvidenceUse` records. Moreover, `DIRECT` evidence requires the consumer and evidence to have identical subjects, which generally prevents composition of certificates for different inputs; bridge mediation is defined only for inference. The model therefore permits an unsupported composition Claim while making ordinary multi-subject certificate composition difficult or impossible to express. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:248>) [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:286>)

- **The selected object/policy is not bound to its generating chain.** Its entire well-formedness condition is `subject_ref`. It need not cite a selection application, \(Q_\psi\), composition Claim, or certificate, and it need not have a nonempty selection-operator projection. Thus an arbitrary subject can be inserted immediately before post-hoc inference. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:250>)

## HIGH

- **Certificate identity remains under-typed.** `source_cert_claim_digest` is required to resolve to a “cited certificate Claim,” but no invariant defines which `chain_segment` or certificate kind qualifies. Acceptance is delegated to configurable schema lists and segment pairs, and a schema may apply to multiple segments. This does not prevent stability, perturbation, composition, bridge, and inference Claims from being admitted in the wrong evidence role. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:260>) [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:464>)

- **Composition and selection operators are conflated.** The specification requires `composition_operator_digest = subject_ref.selection_operator_digest`. A rule for combining guarantees—such as union-bound, sequential, or adaptive composition—is mathematically different from the selection operator producing the selected object. A separate typed `CertificateCompositionRule` is needed. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:244>)

- **Post-hoc inference endpoints are not coherently typed.** `I-BRIDGE-01` equates `target_digest` with the inference-target digest, while `I-CERT-02b` equates it with the consumer’s subject digest. No invariant defines the inference target as that subject, so a proposition, statistic, or inferential estimand is conflated with an object/policy subject. In addition, `TransformObject` says endpoint “payloads validate,” but stores no payload references; the NetRequired-closure and failure-budget digest constructions are also undefined. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:260>) [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:265>)

- **Mathematical identity omits required assumptions.** `claim_math_fingerprint` intentionally excludes `NetRequired`. Hence certificates with the same conclusion and guarantee but different assumptions receive the same fingerprint, and a counterexample automatically propagates between them. A counterexample under \(A\) need not refute the same conclusion under an additional assumption \(B\) that excludes its construction. The NetRequired-closure digest must be part of mathematical identity or the conclusion must explicitly internalize all assumptions. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:193>) [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:364>)

- **The free-text theorem prohibition is normative but not structurally checkable.** `I-DEF-03` states the correct prohibition, but `SchemaRegistryEntry` exposes only a `forbids_opaque_string_roots` flag—apparently self-attested—and no schema-role discriminator or recursively derived “no opaque mathematical text” result. The same registry is also used for assertions, definitions, subjects, domains, operators, derivations, and proof bodies without role-specific admission rules. Thus the object model does not itself demonstrate that nested free-text theorem carriers are rejected. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:108>) [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:464>)

## MEDIUM

- **`selection_stability` and `perturbation` have distinct tags but identical certificate constraints.** There is no required guarantee-kind discriminator establishing whether the payload is a perturbation lemma, a stability theorem, or an end-user certificate. Schema configuration might separate them, but the kernel does not. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:248>)

- **`EvidenceUse` is insufficiently identified.** It has no evidence-slot identifier, digest recomputation formula, uniqueness rule, or prohibition against simultaneous conflicting `DIRECT` and `BRIDGE_TRANSFORM` records for the same citation. This makes certificate-role validation ambiguous even where evidence is supplied. [CANONICAL_OBJECTS.md](</Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:286>)

MATH_VERDICT: FAIL
