- **CRITICAL — Assumption discharge is unscoped and circular.** `DischargeRecord` lacks the claim/use it discharges, yet I-ASM-01 subtracts records from a claim’s closure. A record can therefore remove an assumption from unrelated claims. The discharger may also depend transitively on the same assumption, enabling circular discharge. Add a target claim/use binding and require the discharger’s complete closure to exclude the discharged assumption. [CANONICAL_OBJECTS.md:128](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:128)

- **CRITICAL — Proof laundering remains possible.** Nothing explicitly requires `ProofAttachment.claim_digest == ProofEvidence.claim_digest`; hashing an arbitrary pair does not establish that equality. Additionally, `kind` and `body_digest` lack a typed, resolvable proof-body object and validation rule. Require claim equality, body existence/digest recomputation, a closed proof-kind vocabulary, and typed validation before proof-status advancement. [CANONICAL_OBJECTS.md:171](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:171)

- **HIGH — Empirical bypass remains.** §4.5 applies only when `assertion_payload` explicitly “cites empirical support objects.” Empirical values can be embedded directly, while `parameter_domain.data_dependence.evidence_digest` remains an unconstrained non-Claim digest outside dependency/assumption closure. Require every normative empirical/evidence slot—including parameter-domain evidence—to resolve to an essential Claim/Assumption edge or be schema-declared non-essential and barred from promotion. [CANONICAL_OBJECTS.md:204](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:204)

- **HIGH — Certificate binding is incomplete across consumers.** I-CERT-01 binds a certificate Claim internally, but I-OBJ-07 requires only a matching subject digest when that Claim is used as promotion evidence. The same subject can therefore reuse a certificate from a different mechanism, parameter domain, mathematical scope, or guarantee context. Define a typed evidence-use edge binding the target Claim and certificate Claim with equality over all applicability fields. [CANONICAL_OBJECTS.md:192](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:192)

- **HIGH — DefPin coverage still binds bare symbol names, not identifier occurrences to definition families.** An identifier can be “covered” by any pinned definition containing the same symbol because no AST occurrence→`def_id` mapping is required. Coverage of non-payload normative mathematical fields is also unspecified, while `symbol_inventory_rule_id` conflicts with the claimed fixed walk. Require definition-qualified identifier nodes or a canonical occurrence mapping and inventory all normative mathematical fields. [CANONICAL_OBJECTS.md:89](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:89)

Iteration-1 ready for Iter2? NO
