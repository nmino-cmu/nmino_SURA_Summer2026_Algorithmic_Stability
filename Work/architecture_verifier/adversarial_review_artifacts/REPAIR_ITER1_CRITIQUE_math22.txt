- **CRITICAL — Chain continuity is only nominal.** Dependencies require the preceding segment type, but no invariant equates adjacent endpoints: data regime, score family, mechanism/operator, selected subject, domain, and scope may disagree. In particular, stability need not match its `selection_application` predecessor. Unrelated claims can therefore form a “complete” chain. [CANONICAL_OBJECTS.md:230](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:230)

- **CRITICAL — Certificate composition does not bind rule application.** `I-COMP-02` checks only certificate-segment membership and EvidenceUse slots. Nothing requires the composition rule to accept the inputs, checks compatibility among their mechanisms/domains/scopes, or binds the composed subject and guarantee to the rule’s computed output. Arbitrary certificates can be combined into an arbitrary composition claim. [CANONICAL_OBJECTS.md:247](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:247)

- **CRITICAL — Bridge endpoints are disconnected.** The source subject is bound to the source certificate, while the consumer subject is independently described as the selected object; no equality or typed subject transformation connects them. Additionally, the three TransformObjects are not required to have input/output digests equal to the source certificate and inference endpoints. A certificate about object A can therefore bridge to inference about object B. [CANONICAL_OBJECTS.md:263](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:263)

- **HIGH — `selected_object` does not establish certificate coverage.** It binds only subject equality to either a selection application or a composition. The first option permits the stability/composition portion of the declared chain to be skipped; the second inherits composition’s unconstrained output. No certificate digest, EvidenceUse, or matching mechanism/domain/scope is required. [CANONICAL_OBJECTS.md:239](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:239)

- **HIGH — Assumption closure is not normatively defined.** `GrossRequired(C)` and `NetRequired(C)` are specified only as “as before.” The document does not define which roles enter gross requirements, how prerequisite closure interacts with discharge, or set-versus-multiset treatment. Consequently, fingerprints and assumption transforms cannot be implemented uniquely from ITER1.22. [CANONICAL_OBJECTS.md:142](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:142)

- **HIGH — The mathematical fingerprint omits definition semantics.** Claim-level `definition_pins[]` are absent from the fingerprint. Claims with identical encoded syntax but different pinned meanings can receive the same fingerprint, making `RENAMES` and counterexample propagation identify mathematically different claims. `guarantee_core` also lacks a canonical extraction rule. [CANONICAL_OBJECTS.md:179](/Users/nicholasmino/Desktop/Research/Work/architecture/07-schemas/CANONICAL_OBJECTS.md:179)

MATH_VERDICT: FAIL
