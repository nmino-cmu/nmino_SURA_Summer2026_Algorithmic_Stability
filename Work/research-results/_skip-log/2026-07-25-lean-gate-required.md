# Policy: Lean gate required for papers (2026-07-25)

Publication policy updated: `derived_lean_status = LEAN_FULL` is mandatory before
generating or regenerating `research-results/**/paper.*`.

Computational-only PASS (`*_COMPUTATIONAL_V1`, `DEMO_*`) does **not** authorize papers.
Prior computational papers moved to `_archive/v0-computational/` and regenerated only
after Lean certificates showed LEAN_FULL for argmax margin and both thresholding theorems.
